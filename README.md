# NGINX Static Website (Docker Project)

A simple static website served using **Nginx** inside a **Docker container**.
This projcet demonstrates the basics of Docker image creation, container execution, and serving static HTML content.